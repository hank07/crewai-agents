# LLM API key (required)
OPENAI_API_KEY=your-openai-api-key

# Optional: custom model and endpoint
LLM_MODEL=gpt-4o-mini
LLM_API_KEY=
LLM_BASE_URL=

# YouTube search tool needs OpenAI embeddings
# so OPENAI_API_KEY above is also used for the YouTube tool
